# -*- coding: utf-8 -*-
"""student_dataset_nb_ML_Class_Practice_Challenge.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1Xu9IN0h1rNj2COzpi0wbwz57MPFPxUsJ

# 🧠 Machine Learning Class Challenge: Build ML Model

## Dataset
This challenge uses the attached dataset **`student_dataset_nb.csv`**.


> **Important:** The target column is only a suggested starting point. As an AI/ML student, you should inspect the dataset and confirm whether it is appropriate before modeling.

---

## 🎯 Challenge Objective

Your task is to develop an end-to-end machine learning solution that can learn from this dataset and make predictions for the target variable.

You are expected to demonstrate the complete ML workflow:

**Problem Understanding → Data Understanding → Data Cleaning → EDA → Feature Engineering → Train/Test Split → Preprocessing → Model Training → Evaluation → Model Comparison → Final Recommendation**

### Your role
Imagine you are a junior machine learning engineer given this dataset by a client. Your job is to:

1. Understand the business/data problem.
2. Identify the prediction target.
3. Explore the data.
4. Clean and prepare the data.
5. Build at least **2 machine learning models**.
6. Evaluate and compare the models.
7. Select a final model based on evidence.
8. Explain your findings in simple business language.

### Rules
- Do **not** jump directly to model training.
- Inspect the dataset before making assumptions.
- Avoid data leakage.
- Use a reproducible train/test split.
- Explain important decisions with Markdown.
- Do not judge a model only by accuracy if another metric is more appropriate.

## 1. Import Libraries

Import the libraries you need. You may use additional libraries when necessary.
"""

# TODO: Import the libraries you need

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt

# Add other imports as needed

"""## 2. Load the Dataset

Load `student_dataset_nb.csv` and store it in a DataFrame called `df`.
"""

# TODO: Load the dataset

df = pd.read_csv("student_dataset_nb.csv")

# Display the first 5 rows
df.head()

"""## 3. Data Understanding

Before modeling, investigate the structure and quality of the data.

### Tasks
- Display the number of rows and columns.
- Inspect column names.
- Check data types.
- Check for missing values.
- Check for duplicated rows.
- Generate descriptive statistics.
- Identify categorical and numerical columns.
- Inspect the target variable.

### Questions to answer in Markdown
1. What does each important variable appear to represent?
2. Which columns are numerical?
3. Which columns are categorical?
4. Are there missing values?
5. Are there duplicates?
6. What is the target variable?
7. Is the problem classification or regression? Explain why.

"""

# TODO: Explore the dataset

print("Shape:", df.shape)
print("\nColumns:")
print(df.columns.tolist())

print("\nData types:")
display(df.dtypes)

print("\nMissing values:")
display(df.isna().sum())

print("\nDuplicate rows:", df.duplicated().sum())

print("\nDescriptive statistics:")
display(df.describe(include="all").T)

"""### Suggested target

Start by investigating **`Pass_Exam`**, but do not blindly assume it is the correct target.

Use the following checks:

- What values does it contain?
- How many unique values are there?
- Is it continuous or categorical?
- Does it represent something we could reasonably predict from the other columns?


"""

# TODO: Inspect the suggested target

TARGET = "Pass_Exam"

print("Target:", TARGET)
print("Unique values:", df[TARGET].nunique())
display(df[TARGET].value_counts(dropna=False).head(20))

"""## 4. Exploratory Data Analysis (EDA)

Create visualizations that help you understand the dataset.

### Minimum requirements
Create at least:
- 2 plots for individual variables/distributions
- 1 plot showing the target distribution
- 2 plots showing relationships between features and the target
- A correlation heatmap if the dataset contains enough numerical variables

### Questions
- Are there obvious outliers?
- Are some classes highly imbalanced?
- Which variables appear related to the target?
- Do you see anything that could create data leakage?
- Which variables might be useful predictors?

"""

# TODO: Create EDA visualizations

# Example:
# df.hist(figsize=(12, 8))
# plt.tight_layout()
# plt.show()

# Add your own plots here.

"""## 5. Data Cleaning

Prepare the data for machine learning.

### Tasks
- Handle missing values appropriately.
- Remove duplicates where justified.
- Correct inappropriate data types where necessary.
- Investigate obvious outliers.
- Decide whether any columns should be excluded.

> Do not automatically delete every row containing a missing value. Explain your choice.

### Deliverable
Write a short Markdown explanation describing what you cleaned and why.
"""

# TODO: Perform data cleaning

# Make a copy so the original loaded data is preserved.
data = df.copy()

# Your cleaning steps here

"""## 6. Define Features (X) and Target (y)

Separate the predictor variables from the target.

> **Important:** Do not include identifiers or columns that reveal the target directly. Explain any columns you remove.
"""

# TODO: Define X and y

TARGET = "Pass_Exam"

# Example:
# X = data.drop(columns=[TARGET])
# y = data[TARGET]

X = data.drop(columns=[TARGET])
y = data[TARGET]

print("X shape:", X.shape)
print("y shape:", y.shape)

"""## 7. Train/Test Split

Split the data into training and testing sets.

### Requirements
- Use a sensible test size such as 20%.
- Use `random_state=42` for reproducibility.
- If this is a classification problem, consider using `stratify=y` when appropriate.

**Question:** Why should we keep the test set separate from model training?
"""

# TODO: Split the dataset

from sklearn.model_selection import train_test_split

# If classification, consider:
# X_train, X_test, y_train, y_test = train_test_split(
#     X, y, test_size=0.20, random_state=42, stratify=y
# )

# If regression, omit stratify.

"""## 8. Preprocessing

Prepare numerical and categorical variables.

Consider:
- Missing-value imputation
- Standardization/scaling of numerical features where appropriate
- One-hot encoding of categorical features

Use a **Pipeline** and **ColumnTransformer** where appropriate so that preprocessing is learned only from the training data.

### Question
Why is it dangerous to calculate preprocessing statistics using the complete dataset before the train/test split?
"""

# TODO: Identify numerical and categorical features

from sklearn.compose import ColumnTransformer
from sklearn.pipeline import Pipeline
from sklearn.impute import SimpleImputer
from sklearn.preprocessing import OneHotEncoder, StandardScaler

numeric_features = X.select_dtypes(include=np.number).columns.tolist()
categorical_features = X.select_dtypes(exclude=np.number).columns.tolist()

print("Numerical features:", numeric_features)
print("Categorical features:", categorical_features)

numeric_transformer = Pipeline(steps=[
    ("imputer", SimpleImputer(strategy="median")),
    ("scaler", StandardScaler())
])

categorical_transformer = Pipeline(steps=[
    ("imputer", SimpleImputer(strategy="most_frequent")),
    ("onehot", OneHotEncoder(handle_unknown="ignore"))
])

preprocessor = ColumnTransformer(
    transformers=[
        ("num", numeric_transformer, numeric_features),
        ("cat", categorical_transformer, categorical_features)
    ]
)

"""## 9. Build Model 1 — Baseline

Start with a simple baseline model.

Choose an appropriate baseline based on whether your problem is:

- **Classification:** Logistic Regression, Decision Tree, or similar.
- **Regression:** Linear Regression, Ridge, or similar.

### Requirement
Explain why your selected baseline is appropriate.

"""

# TODO: Build your first model

# Example for classification:
# from sklearn.linear_model import LogisticRegression
# model1 = LogisticRegression(max_iter=1000)

# Example for regression:
# from sklearn.linear_model import LinearRegression
# model1 = LinearRegression()

# Build a pipeline:
# model1_pipeline = Pipeline([
#     ("preprocessor", preprocessor),
#     ("model", model1)
# ])

# Train the model
# model1_pipeline.fit(X_train, y_train)

"""## 10. Build Model 2 — More Powerful Model

Build a second model and compare it with your baseline.

Possible choices:

### Classification
- Random Forest
- Gradient Boosting
- Support Vector Machine
- K-Nearest Neighbors

### Regression
- Random Forest Regressor
- Gradient Boosting Regressor
- Random Forest / Extra Trees
- Ridge/Lasso

Do not choose a model just because it is more complex. Explain why it is worth testing.

"""

# TODO: Build your second model

# Example:
# from sklearn.ensemble import RandomForestClassifier
# model2 = RandomForestClassifier(n_estimators=200, random_state=42)

# model2_pipeline = Pipeline([
#     ("preprocessor", preprocessor),
#     ("model", model2)
# ])

# model2_pipeline.fit(X_train, y_train)

"""## 11. Evaluate the Models

Evaluate both models on the **test set**.

### If Classification
Calculate at least:
- Accuracy
- Precision
- Recall
- F1-score
- Confusion matrix

Also consider ROC-AUC when the problem and model support it.

### If Regression
Calculate at least:
- MAE
- MSE
- RMSE
- R²

### Important
Explain what each metric tells us and why your selected primary metric makes sense for this problem.

"""

# TODO: Generate predictions

# y_pred1 = model1_pipeline.predict(X_test)
# y_pred2 = model2_pipeline.predict(X_test)

# Add appropriate evaluation metrics below.

"""## 12. Model Comparison

Create a small comparison table containing the evaluation metrics for both models.

Then answer:

1. Which model performed differently across the metrics?
2. Is one model consistently stronger, or is there a trade-off?
3. Which metric matters most for this problem?
4. Is there evidence of overfitting?
5. What would you investigate before deploying the model?

"""

# TODO: Create a model comparison DataFrame

# Example structure:
# comparison = pd.DataFrame({
#     "Model": ["Model 1", "Model 2"],
#     "Metric 1": [score1, score2],
#     "Metric 2": [score1b, score2b]
# })
# display(comparison)

"""## 13. Error Analysis

Do not stop at the metric.

Investigate examples where the model makes incorrect predictions.

### Questions
- Which observations are difficult for the model?
- Are errors concentrated in a particular class or range?
- Are there suspicious observations?
- Could better feature engineering improve performance?
- Would collecting more data help?

Show at least one useful error-analysis table or visualization.

"""

# TODO: Perform error analysis

# Create a DataFrame containing:
# - actual target
# - predicted target
# - relevant input features
# Then inspect incorrect predictions.

"""## 14. Feature Importance / Model Interpretation

Try to explain which features contribute most to the model's predictions.

Depending on the model, you may use:

- Feature coefficients
- Feature importance
- Permutation importance

### Questions
- Which features appear most important?
- Does this make sense from a real-world perspective?
- Could any important feature cause leakage or unfair prediction?

"""

# TODO: Analyze feature importance or model coefficients

# Choose an appropriate interpretation method for your final model.

"""## 15. Optional Extension — Hyperparameter Tuning ⭐

For students who finish early:

Use `GridSearchCV` or `RandomizedSearchCV` to improve one of your models.

### Requirements
- Tune at least 2–3 meaningful hyperparameters.
- Use cross-validation.
- Compare the tuned model with the original model.
- Explain whether the improvement is meaningful.

**Do not tune simply to obtain a higher score.**

"""

# TODO: Optional hyperparameter tuning

# from sklearn.model_selection import GridSearchCV

# Define a parameter grid and perform cross-validation.

"""# 🏁 Final Student Report

Complete the following section in your own words.

## A. Problem Definition
What problem were you solving?

## B. Target Variable
What did you predict, and why?

## C. Data Quality
What data-quality issues did you discover and how did you handle them?

## D. EDA Findings
List at least 3 important findings from your exploratory analysis.

## E. Models
Which models did you train and why?

## F. Evaluation
Present your evaluation metrics and explain what they mean.

## G. Error Analysis
What types of predictions were difficult for your model?

## H. Final Model
Which model would you take forward for further testing, and what evidence supports that choice?

## I. Limitations
List at least 3 limitations of your solution.

## J. Next Steps
If you had one more week, what would you improve?

---

# 🎓 Challenge Deliverables

Submit:

- [ ] Completed Jupyter Notebook
- [ ] At least 2 trained ML models
- [ ] EDA visualizations
- [ ] Data-cleaning explanation
- [ ] Model evaluation metrics
- [ ] Model comparison
- [ ] Error analysis
- [ ] Feature interpretation
- [ ] Final written conclusion

## Bonus Challenge 🚀

Try one or more of the following:

1. Cross-validation
2. Hyperparameter tuning
3. Feature engineering
4. Handling class imbalance (if applicable)
5. SHAP or permutation importance
6. Saving the final model with `joblib`
7. Creating a simple prediction function
8. Building a small Streamlit interface

---

# 👨‍🏫 Instructor Rubric

| Area | Points |
|---|---:|
| Data loading & understanding | 10 |
| Data cleaning | 10 |
| EDA & visualizations | 15 |
| Feature/target preparation | 10 |
| Preprocessing & leakage prevention | 10 |
| Model 1 | 10 |
| Model 2 | 10 |
| Evaluation & metrics | 10 |
| Error analysis & interpretation | 5 |
| Final explanation/recommendation | 10 |
| **Total** | **100** |

### Suggested grading standard

**90–100:** Excellent — complete workflow, strong reasoning, clear interpretation  
**75–89:** Good — correct workflow with minor gaps  
**60–74:** Developing — model works but analysis or reasoning is incomplete  
**Below 60:** Needs improvement — major workflow or modeling issues

> Focus grading on **reasoning and methodology**, not just the final model score.
"""